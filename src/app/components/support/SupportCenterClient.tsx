'use client'
import { useState } from 'react'
import SupportRequestForm from './SupportRequestForm'
import MyTickets from './MyTickets'

export default function SupportCenterClient(){const[view,setView]=useState<'home'|'ticket'|'feedback'>('home');return <div className="space-y-5">{view==='home'?<><div className="grid gap-4 md:grid-cols-2"><button onClick={()=>setView('ticket')} className="rounded-2xl bg-[#010521] p-6 text-left text-white shadow-sm transition hover:-translate-y-0.5"><span className="text-2xl">🎫</span><h2 className="mt-4 text-lg font-semibold">Create support ticket</h2><p className="mt-1 text-sm leading-6 text-white/60">For account, order, payout, or website problems. You can attach a screenshot.</p><span className="mt-5 inline-block text-sm font-semibold text-[#E5C56A]">Get help →</span></button><button onClick={()=>setView('feedback')} className="rounded-2xl border border-[#0D1B3E]/10 bg-white p-6 text-left shadow-sm transition hover:-translate-y-0.5"><span className="text-2xl">💡</span><h2 className="mt-4 text-lg font-semibold text-[#0D1B3E]">Send feedback or suggestion</h2><p className="mt-1 text-sm leading-6 text-gray-500">Share an idea or tell us how Hiroma can improve.</p><span className="mt-5 inline-block text-sm font-semibold text-[#9a741f]">Share feedback →</span></button></div><MyTickets/></>:<section className="rounded-2xl border border-[#0D1B3E]/10 bg-white p-6 shadow-sm"><button onClick={()=>setView('home')} className="mb-5 text-sm font-semibold text-[#9a741f]">← Back to Support Center</button><h2 className="text-lg font-semibold text-[#0D1B3E]">{view==='ticket'?'Create a support ticket':'Send feedback or suggestion'}</h2><p className="mt-1 mb-5 text-sm text-gray-500">{view==='ticket'?'Our Hiroma support team will review your concern and update you here.':'Your idea goes directly to the Hiroma team.'}</p><SupportRequestForm member mode={view==='ticket'?'ticket':'feedback'} onDone={()=>setView('home')}/></section>}</div>}
